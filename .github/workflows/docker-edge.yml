name: Publish edge image

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout cashctl
        uses: actions/checkout@v4
        with:
          path: cashctl

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: cashctl/go.mod

      - name: Build linux/amd64 binary
        working-directory: cashctl
        env:
          CGO_ENABLED: 0
          GOOS: linux
          GOARCH: amd64
        run: |
          mkdir -p ../dist
          go build -o ../dist/cashctl .

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: dist
          file: cashctl/build/cashctl/Dockerfile.release
          push: true
          tags: |
            ghcr.io/ohstr/cashctl:edge
            ghcr.io/ohstr/cashctl:${{ github.sha }}
