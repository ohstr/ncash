name: Publish edge image

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ncash
        uses: actions/checkout@v4
        with:
          path: ncash

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ncash/go.mod

      - name: Build linux/amd64 binary
        working-directory: ncash
        env:
          CGO_ENABLED: 0
          GOOS: linux
          GOARCH: amd64
        run: |
          mkdir -p ../dist
          go build -o ../dist/ncash .

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: dist
          file: ncash/build/ncash/Dockerfile.release
          push: true
          tags: |
            ghcr.io/ohstr/ncash:edge
            ghcr.io/ohstr/ncash:${{ github.sha }}
